En Docker

Borrar todos los containers, imágenes y networks.

En la terminal

docker pull mysql
docker pull eclipse-temurin
docker network create spring-net
docker run -d --name mysql -p 3306:3306 --net spring-net -e MYSQL_ROOT_PASSWORD=my-secret-pw -v db:/var/lib/mysql mysql
docker network connect spring-net mysql

En MySQL WorkBench

anime.sql

En la terminal

docker build -t crud .
docker run -d --name crud -p 8080:8080 --net spring-net -e MYSQL_HOST=mysql -e MYSQL_USER=root -e MYSQL_PASSWORD=my-secret-pw -e MYSQL_PORT=3306 crud



